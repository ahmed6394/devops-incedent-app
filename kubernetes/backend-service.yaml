apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend
  name: backend
  namespace: my-namespace
spec:
  ports:
  - name: http
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: backend-deployment
  type: ClusterIP
